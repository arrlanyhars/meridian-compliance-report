# Copy this to .env and fill in your key, or export it directly in your shell.
# Free-tier key: https://aistudio.google.com/app/apikey
#
# The report itself does not depend on this. If it's missing, run_report.py
# still produces the full numeric report and audit trail, it just skips the
# narrative paragraph and marks it SKIPPED_NO_API_KEY in the output.
GOOGLE_API_KEY=
